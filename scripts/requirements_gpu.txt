# GPU-Accelerated Requirements for DoggoBot

# Core dependencies
requests==2.31.0
opencv-python==4.8.1.78
numpy==1.24.3
Pillow==10.1.0

# Face recognition (CPU-based matching)
face-recognition==1.3.0
dlib==19.24.2

# YOLOv8 + PyTorch with CUDA (GPU acceleration)
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
